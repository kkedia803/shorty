services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env

  frontend:
    build: 
      context: ./frontend
      args: 
        VITE_API_URL : http://localhost:3000
    ports:
      - "4173:4173"
    env_file:
      - ./frontend/.env